Just a simple little script I wrote while tinkering with [`nannou`](https://nannou.cc/).

Running this should be a simple case of cloning the repository, `cd`ing into it, and running `cargo run --release`.